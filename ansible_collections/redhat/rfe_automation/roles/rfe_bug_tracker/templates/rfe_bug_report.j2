# RFE/Bug Tracker Report - {{ customer_name }}

**Generated:** {{ report_timestamp }}  
**Customer:** {{ customer_name }} ({{ account_name }})  
**Account Number:** {{ account_number }}  

## 📊 Executive Summary

- **Total Cases Analyzed:** {{ total_cases }}
- **Active RFEs:** {{ active_rfes | length }}
- **Active Bugs:** {{ active_bugs | length }}
- **Closed Cases:** {{ closed_cases | length }}
- **Data Quality Score:** {{ (data_quality_score | float * 100) | round(1) }}%

## 🎯 Active RFE Cases ({{ active_rfes | length }})

{% if active_rfes | length > 0 %}
| Case Number | Subject | Severity | Status |
|-------------|---------|----------|--------|
{% for case in active_rfes %}
| {{ case.caseNumber }} | {{ case.subject[:80] }}{% if case.subject | length > 80 %}...{% endif %} | {{ case.severity if case.severity else 'N/A' }} | {{ case.internalStatus if case.internalStatus else 'N/A' }} |
{% endfor %}
{% else %}
No active RFE cases found.
{% endif %}

## 🐛 Active Bug Cases ({{ active_bugs | length }})

{% if active_bugs | length > 0 %}
| Case Number | Subject | Severity | Status |
|-------------|---------|----------|--------|
{% for case in active_bugs %}
| {{ case.caseNumber }} | {{ case.subject[:80] }}{% if case.subject | length > 80 %}...{% endif %} | {{ case.severity if case.severity else 'N/A' }} | {{ case.internalStatus if case.internalStatus else 'N/A' }} |
{% endfor %}
{% else %}
No active bug cases found.
{% endif %}

## 📋 Closed Cases ({{ closed_cases | length }})

{% if closed_cases | length > 0 %}
| Case Number | Subject | Type | Closed Date |
|-------------|---------|------|-------------|
{% for case in closed_cases %}
| {{ case.caseNumber }} | {{ case.subject[:60] }}{% if case.subject | length > 60 %}...{% endif %} | {{ case.caseType if case.caseType else 'N/A' }} | {{ case.lastUpdateDate if case.lastUpdateDate else 'N/A' }} |
{% endfor %}
{% else %}
No closed cases found.
{% endif %}

## 📈 Data Quality Report

- **Data Quality Score:** {{ (data_quality_score | float * 100) | round(1) }}%
- **Total Cases Processed:** {{ total_cases }}
- **Validation Status:** {% if (data_quality_score | float) >= 0.95 %}✅ Passed{% else %}❌ Failed{% endif %}

---

*This report was automatically generated by the Red Hat RFE Automation System.*  
*For questions or issues, please contact your TAM or the TAM Operations team.*
