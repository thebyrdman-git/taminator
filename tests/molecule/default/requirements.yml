---
# Molecule test requirements
collections:
  - name: community.general
    version: ">=7.0.0"
  - name: ansible.posix
    version: ">=1.4.0"
  - name: community.docker
    version: ">=3.0.0"

roles:
  - name: geerlingguy.pip
    version: ">=2.0.0"
