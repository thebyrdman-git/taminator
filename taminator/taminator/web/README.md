# Web UI assets

Place **taminator_logo.png** in this directory. It is used by:

- The browser UI (header logo)
- The desktop application build (Electron icon); see `gui/package.json` which points to `../web/taminator_logo.png`

Copy your logo file here (e.g. from Downloads) so both the web UI and the packaged app use the same image.
