# Open Troubleshooting Cases - {{ customer_name | default('Unknown Customer') }}

**Generated:** {{ ansible_date_time.date | default('Unknown Date') }}  
**Account:** {{ account_number | default('Unknown Account') }}

## 🔧 Open Cases for Troubleshooting
{% if all_active_cases is defined and all_active_cases | length > 0 %}
| Case # | Subject | Status | Priority | SBR Group |
|--------|---------|--------|----------|-----------|
{% for case in all_active_cases %}
| {{ case.caseNumber | default('N/A') }} | {{ case.subject | default('No Subject') | truncate(50) }} | {{ case.internalStatus | default('Unknown') }} | {{ case.priority | default('N/A') }} | {{ case.sbrGroup | default('N/A') }} |
{% endfor %}

**Total Open Cases:** {{ all_active_cases | length }}
{% else %}
No open troubleshooting cases found.
{% endif %}

## 🎯 Priority Component Cases
{% if priority_active_cases is defined and priority_active_cases | length > 0 %}
| Case # | Subject | Status | Priority |
|--------|---------|--------|----------|
{% for case in priority_active_cases %}
| {{ case.caseNumber | default('N/A') }} | {{ case.subject | default('No Subject') | truncate(50) }} | {{ case.internalStatus | default('Unknown') }} | {{ case.priority | default('N/A') }} |
{% endfor %}

**Priority Cases:** {{ priority_active_cases | length }}
{% else %}
No priority component cases found.
{% endif %}

---
*Report generated by RFE Automation Tool*
