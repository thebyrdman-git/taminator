# External Tracker Summary - {{ customer_name | default('Unknown Customer') }}

**Generated:** {{ ansible_date_time.date | default('Unknown Date') }}  
**Account:** {{ account_number | default('Unknown Account') }}

## 🔗 Cases with External Trackers

{% if external_tracker_cases is defined and external_tracker_cases | length > 0 %}
These cases are tracked in external systems (JIRA, Bugzilla, etc.) and are excluded from the Active Cases Report to avoid duplication.

| Case # | Subject | External Trackers | Status |
|--------|---------|-------------------|--------|
{% for case in external_tracker_cases %}
| {{ case.caseNumber | default('N/A') }} | {{ case.subject | default('No Subject') | truncate(60) }} | {{ case.external_trackers | length }} tracker(s) | {{ case.status | default('Unknown') }} |
{% endfor %}

### 📋 Detailed External Tracker Information

{% for case in external_tracker_cases %}
#### Case {{ case.caseNumber }}: {{ case.subject | truncate(80) }}

**External Trackers:**
{% if case.external_trackers | length > 0 %}
{% for tracker in case.external_trackers %}
- **{{ tracker.get('tracker_type', 'Unknown') }}**: {{ tracker.get('tracker_id', 'N/A') }}
  - URL: {{ tracker.get('url', 'N/A') }}
  - Status: {{ tracker.get('status', 'Unknown') }}
  - Last Updated: {{ tracker.get('last_updated', 'Unknown') }}
{% endfor %}
{% else %}
No external tracker details available.
{% endif %}

---
{% endfor %}

**Total Cases with External Trackers:** {{ external_tracker_cases | length }}
{% else %}
No cases with external trackers found.
{% endif %}

## 📊 Summary

- **Cases with External Trackers**: {{ external_tracker_cases | length }}
- **These cases are excluded** from the Active Cases Report to prevent duplication
- **External trackers are managed** in JIRA, Bugzilla, or other external systems
- **Case status and details** are available in the external tracking systems

---
*Report generated by RFE Automation Tool using Customer Portal API*
