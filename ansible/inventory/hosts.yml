---
all:
  children:
    # Infrastructure hosts
    infrastructure:
      hosts:
        miraclemax:
          ansible_host: 192.168.1.34
          ansible_user: jbyrd
          ansible_python_interpreter: /usr/bin/python3
          
          # Host-specific vars
          miraclemax_services_enabled:
            - actual-budget
            - n8n
            - redis
            - homer
            - portainer
            - netdata
            - grafana
            - prometheus
            - loki
            - traefik
    
    # Local development
    localhost:
      hosts:
        127.0.0.1:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
