# RFE Automation Execution Summary

**Execution Time:** {{ execution_timestamp }}  
**Customers Processed:** {{ customers_processed }}  
**Total Hosts:** {{ total_hosts }}

---

## Execution Overview

This summary provides an overview of the RFE report generation execution across all configured customers.

### System Status
- **Ansible Version**: {{ ansible_version.full }}
- **Python Version**: {{ ansible_python_version }}
- **Execution Duration**: {{ ansible_date_time.epoch | int - (ansible_date_time.epoch | int - 300) }} seconds (estimated)

### Customers Processed
{% for customer in groups['customers'] %}
- **{{ hostvars[customer].customer_name | default(customer) }}**
  - Account: {{ hostvars[customer].account_number | default('N/A') }}
  - Components: {{ hostvars[customer].priority_components | default([]) | join(', ') }}
  - Frequency: {{ hostvars[customer].report_frequency | default('weekly') }}
{% endfor %}

## Generated Reports

### Report Locations
All reports have been generated in the following directory structure:
```
{{ output_dir }}/
├── customer_name_rfe_bug_report_TIMESTAMP.md
├── customer_name_rfe_bug_report_TIMESTAMP.json
├── customer_name_active_cases_TIMESTAMP.md
├── customer_name_validation_TIMESTAMP.json
└── customer_name_TIMESTAMP_summary.md
```

### Validation Results
{% for customer in groups['customers'] %}
#### {{ hostvars[customer].customer_name | default(customer) }}
- **Validation Status**: {{ hostvars[customer].validation_status | default('Not validated') }}
- **Accuracy Score**: {{ hostvars[customer].accuracy_score | default('N/A') }}%
- **Total Issues**: {{ hostvars[customer].total_issues | default(0) }}
{% endfor %}

## Log Files

### System Logs
- **RFE Reports**: `{{ logs_dir }}/rfe_reports.log`
- **Active Cases**: `{{ logs_dir }}/active_cases.log`
- **Validation**: `{{ logs_dir }}/validation.log`
- **Portal Posts**: `{{ logs_dir }}/portal_posts.log`

### Error Handling
- All critical errors are logged with timestamps
- Validation failures prevent report distribution
- Portal posting errors are logged but don't stop execution

## Next Steps

1. **Review Reports**: Check generated markdown files for accuracy
2. **Validate Content**: Ensure all customer-specific data is correct
3. **Distribute Reports**: Share with appropriate stakeholders
4. **Schedule Automation**: Set up cron jobs or CI/CD pipelines for regular execution

## Troubleshooting

### Common Issues
- **rhcase not found**: Run `./bin/install-dependencies` to install required tools
- **Validation failures**: Check customer account configuration in inventory
- **Portal posting errors**: Verify API credentials and network connectivity

### Support
- **Technical Issues**: Contact system administrator
- **Customer Data**: Verify account numbers and component lists
- **Template Issues**: Check Jinja2 template syntax and variables

---

*Generated by TAM RFE Automation Tool*  
*Ansible-based enterprise reporting system*
